---
title: "Next.js 14로 시작하기"
date: "2024-02-03"
description: "Next.js 14의 주요 기능과 시작하는 방법을 알아봅니다."
tags: ["nextjs", "react", "tutorial"]
---

## Next.js 14 시작하기

Next.js는 React 기반의 풀스택 웹 프레임워크입니다.

### 설치

프로젝트를 시작하는 가장 쉬운 방법:

```bash
npx create-next-app@latest my-app
cd my-app
npm run dev
```

### App Router

Next.js 14는 새로운 App Router를 사용합니다:

```typescript
// app/page.tsx
export default function Home() {
  return <h1>Hello, Next.js!</h1>;
}
```

### 서버 컴포넌트

기본적으로 모든 컴포넌트는 서버 컴포넌트입니다:

```typescript
// 서버에서 실행되는 컴포넌트
async function getData() {
  const res = await fetch("https://api.example.com/data");
  return res.json();
}

export default async function Page() {
  const data = await getData();
  return <div>{data.title}</div>;
}
```

### 클라이언트 컴포넌트

상호작용이 필요한 경우 'use client' 사용:

```typescript
"use client";

import { useState } from "react";

export default function Counter() {
  const [count, setCount] = useState(0);
  return <button onClick={() => setCount(count + 1)}>{count}</button>;
}
```

---

Next.js로 멋진 웹 애플리케이션을 만들어보세요!
